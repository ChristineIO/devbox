# Current Feature

## Status

Complete

## Goals

<!-- Bullet points of what success looks like -->

## Notes

<!-- Additional context, constraints, or details from spec -->

## History

<!-- Keep this updated. Earliest to latest. -->

- **2026-04-12:** Scaffolded Next.js 16 project with React 19, Tailwind CSS v4, and TypeScript 5. Stripped default boilerplate, removed unused SVGs, and added project context docs. (Completed)
- **2026-04-12:** Dashboard UI Phase 1 — ShadCN UI initialized with Button and Input components, `/dashboard` route with persistent layout, dark mode by default, top bar with search and New Item/New Collection buttons (display only), sidebar and main placeholders. (Completed)
- **2026-04-13:** Dashboard UI Phase 2 — collapsible sidebar with Types (linked to `/items/[type]`), Favorites and All Collections sections, user avatar footer with settings icon, drawer toggle in top bar, JS-based mobile/desktop switching (rail on desktop, Sheet drawer on mobile). (Completed)
- **2026-04-13:** Dashboard UI Phase 3 — main area with 4 stats cards (items, collections, favorite items, favorite collections), recent collections grid, pinned items list, and 10 most recent items, all wired to `src/lib/mock-data.ts`. (Completed)
- **2026-04-13:** Prisma + Neon setup with initial schema (User, Item, ItemType, Collection, ItemCollection, Tag + NextAuth Account/Session/VerificationToken), migrations configured, and `prisma generate` wired through `prisma.config.ts`. (Completed)
- **2026-04-13:** Seed data — `prisma/seed.ts` creates a bcrypt-hashed demo user, 7 system item types, 5 collections, and 18 items linked via `ItemCollection`; added nullable `User.password` (migration `add_user_password`); `scripts/test-db.ts` extended to print the demo user's collections and items for verification. (Completed)
- **2026-04-13:** Dashboard Collections — replaced mock collections in dashboard main area with live Prisma data via `src/lib/db/collections.ts`; `RecentCollections` and `StatsCards` now async server components; card border color derives from most-used type; type icons rendered from actual types; fixed self-referential `--font-sans` so Geist Sans applies across the app. (Completed)
- **2026-04-13:** Dashboard Items — replaced mock items with live Prisma data via `src/lib/db/items.ts` (`getPinnedItems`, `getRecentItems`, `getItemStats`); `PinnedItems` and `RecentItems` now async server components; `ItemRow` takes embedded type (icon/border color from `ItemType`); `StatsCards` item counts sourced from DB. (Completed)
- **2026-04-13:** Stats & Sidebar — sidebar wired to live DB data: `getSidebarItemTypes` and `getSidebarCollections` feed the dashboard layout, which passes types and collections into `Sidebar`/`SidebarContent`. Types render with DB icons/counts and link to `/items/[typename]`; collections split into Favorites and Recent (color dot from most-used type), with a "View all collections" link. Seed extended with a 6th "Project Notes" collection and three note items; `note` system type recolored from yellow to purple (#a855f7). (Completed)
- **2026-04-14:** Pro Badge — added ShadCN `Badge` component and rendered a subtle outline "PRO" badge next to the `file` and `image` type entries in the sidebar (hidden when collapsed); count pushed to the right with `ml-auto`. (Completed)
- **2026-04-14:** Cleanup — deleted `src/lib/mock-data.ts` (superseded by Prisma); rewrote `/items/[type]/page.tsx` to resolve the type via Prisma by name so the sidebar links work, and added `force-dynamic`. (Completed)
- **2026-04-15:** Audit quick wins — consolidated `getDemoUserId` into `src/lib/db/user.ts` and wrapped it in React `cache()` so repeated calls within one render pass deduplicate; removed duplicates from `items.ts` and `collections.ts`. Dropped manual `useCallback` / `useMemo` from `SidebarContext` (React Compiler handles it). Root `/` now redirects to `/dashboard`. Added composite indexes `@@index([userId, isPinned])` and `@@index([userId, isFavorite])` on `Item` via migration `add_item_boolean_indexes`. (Completed)
- **2026-04-15:** Auth Phase 1 — installed `next-auth@beta` and `@auth/prisma-adapter`; split-config pattern with edge-safe `src/auth.config.ts` (GitHub provider) and full `src/auth.ts` (Prisma adapter + JWT strategy + session callback exposing `user.id`); handlers re-exported from `src/app/api/auth/[...nextauth]/route.ts`; `src/proxy.ts` redirects unauthenticated `/dashboard/*` requests to `/api/auth/signin` with `callbackUrl`; `src/types/next-auth.d.ts` extends `Session.user` with `id`. (Completed)
- **2026-04-15:** Auth Phase 2 — added Credentials provider with split-config pattern: placeholder `authorize: () => null` in `src/auth.config.ts`, real bcrypt validation in `src/auth.ts` (filters the placeholder and appends the real provider). New `POST /api/auth/register` route validates name/email/password/confirmPassword with zod, rejects duplicates (409) and mismatched passwords (400), hashes with bcryptjs cost 12. Installed `zod`. (Completed)
- **2026-04-16:** Auth Phase 3 — custom auth UI replacing NextAuth defaults. New `/sign-in` and `/register` pages under `(auth)` route group with email/password forms, GitHub OAuth button (inline SVG since lucide-react has no `Github` icon), and client-side validation; `RegisterForm` POSTs to `/api/auth/register` then redirects to `/sign-in?registered=1`. Server actions in `src/actions/auth.ts` (`signInWithCredentials`, `signInWithGithub`, `signOutAction`). `auth.config.ts` sets `pages.signIn: "/sign-in"` and `proxy.ts` redirects unauthenticated `/dashboard/*` there. Sidebar footer replaced with `SidebarUserMenu`: reusable `UserAvatar` (GitHub image or initials fallback) linking to `/profile`, plus a base-ui dropdown (`modal={false}`, `render={<Link>}` for Profile, `useTransition` for Sign Out) with Profile + Sign out items. `getSidebarUser` now reads from the live session via `auth()` and includes `image`. Added shadcn `dropdown-menu` and `label`; `suppressHydrationWarning` on `<body>` for browser-extension attribute injection. (Completed)
- **2026-04-17:** Email Verification — installed `resend`; `src/lib/email.ts` sends verification emails via Resend (`onboarding@resend.dev`); `src/lib/db/verification-token.ts` creates/consumes 24h single-use tokens using the existing `VerificationToken` table; `POST /api/auth/register` generates token and sends email after user creation; `GET /api/auth/verify?token=...` consumes token, sets `emailVerified`, redirects to `/sign-in?verified=1`; `POST /api/auth/resend-verification` resends the email; `authorize()` in `src/auth.ts` throws `EmailNotVerifiedError` for unverified credentials users; sign-in UI shows contextual banners (verified, registered, invalid-token) and a "Resend verification email" button on verification errors. (Completed)
- **2026-04-21:** Email Verification Toggle — `ENABLE_EMAIL_VERIFICATION` env var (default `"false"`) read via `src/lib/config.ts`. When off, `POST /api/auth/register` sets `emailVerified` immediately and skips token + Resend; `authorize()` no longer throws `EmailNotVerifiedError`. Register API also returns `verificationRequired`, and `RegisterForm` auto-signs the user in via `signInWithCredentials` and sends them straight to `/dashboard` when verification is off (so the sign-in "check email" banner never shows). All verification code stays in place to flip back on later. Added `scripts/delete-non-demo-users.ts` to clean non-demo users on the dev branch. (Completed)
- **2026-04-29:** Forgot Password — new `/forgot-password` page (email form) and `POST /api/auth/forgot-password` issue 1-hour single-use reset tokens via `src/lib/db/password-reset-token.ts` (reuses `VerificationToken` table with `password-reset:` identifier prefix) and email a `/reset-password?token=...` link via `sendPasswordResetEmail` in `src/lib/email.ts`. `/reset-password` page + `POST /api/auth/reset-password` validate token + new password (zod, min 8, confirm match), bcrypt-hash with cost 12, update `User.password`, consume the token, then redirect to `/sign-in?reset=1`. Sign-in form gets a "Forgot password?" link and an emerald `reset=1` banner. Forgot endpoint always returns `{ success: true }` (no enumeration); email is only sent when the user exists with a credentials password. 60s per-email cooldown enforced server-side (token age check) and mirrored client-side via a `localStorage` countdown that survives navigation. (Completed)
- **2026-04-29:** Profile Page — new `/profile` route (protected via `proxy.ts`) showing `ProfileInfoCard` (avatar, name, email, PRO badge, join date), `ProfileStatsCard` (total items + collections plus per-`ItemType` breakdown so custom types are included), and Account section with `ChangePasswordCard` (credentials users only) and `DeleteAccountCard`. New `getProfileUser` (session-backed, exposes `hasPassword`) and `getProfileStats` in `src/lib/db/user.ts`; `POST /api/account/change-password` verifies current password and re-hashes via bcrypt cost 12; `deleteAccount` server action in `src/actions/account.ts` requires typing "DELETE" + (for credentials users) password, then `prisma.user.delete` (cascades through items/collections/types/Account/Session) and `signOut({ redirectTo: "/sign-in?deleted=1" })`. Confirmation uses base-ui `AlertDialog`. (Completed)
- **2026-05-03:** Rate Limiting for Auth — installed `@upstash/ratelimit` and `@upstash/redis`; new `src/lib/rate-limit.ts` with named sliding-window limiters (`login`, `register`, `forgotPassword`, `resetPassword`, `resendVerification`), `checkRateLimit()` (fail-open if Upstash unconfigured or throws), `getClientIp()` (x-forwarded-for → x-real-ip), and `rateLimitResponse()` returning 429 + JSON + `Retry-After` + `X-RateLimit-*` headers. Wired into `/api/auth/register` (3/1h IP), `/api/auth/forgot-password` (3/1h IP), `/api/auth/reset-password` (5/15min IP), `/api/auth/resend-verification` (3/15min IP+email), and credentials login via `signInWithCredentials` server action (5/15min IP+email — checked before `signIn()` since NextAuth's credentials path is awkward to intercept). Existing inline-error UX surfaces 429 messages on `RegisterForm`/`ResetPasswordForm`/`SignInForm`; forgot-password and resend-verification stay silent by design (anti-enumeration / always-success). `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` added to `.env.example`. (Completed)
- **2026-05-05:** Item List View — `/items/[type]` resolves the type by name and renders a responsive grid (`grid-cols-1 md:grid-cols-2`) of new `ItemCard` components via `getItemsByType(itemTypeId)` in `src/lib/db/items.ts`. `ItemCard` (`src/components/items/ItemCard.tsx`) mirrors the dashboard row style with a 3px left border in `ItemType.color`, type-tinted icon tile, title, pin/star indicators, description, date, and tags. Added `src/app/items/layout.tsx` matching the dashboard/profile chrome (Sidebar + TopBar) so the sidebar persists across `/items/*`, and extended `src/proxy.ts` to protect `/items/*` like `/dashboard` and `/profile`. (Completed)
- **2026-05-09:** Item Drawer — clicking an item now opens a right-side `Sheet` drawer with full detail instead of navigating away. New `getItemById(id)` in `src/lib/db/items.ts` returns an `ItemDetail` shape (card data + content/url/file fields, language, updatedAt, collections); `GET /api/items/[id]` in `src/app/api/items/[id]/route.ts` is auth-checked (401 without session, 404 if not owned). `ItemDrawerProvider`/`useItemDrawer` (`src/components/items/ItemDrawerContext.tsx`) holds the selected card; `ItemDrawer` (`src/components/items/ItemDrawer.tsx`) fetches detail on open with a skeleton loading state, renders an action bar (Favorite, Pin, Copy, Edit, Delete right-aligned — Copy functional, others visual stubs per spec), then title/type/description/content (text→`<pre>`, url→link, file→name+size)/tags/collections/timestamps. `ItemCard` and `ItemRow` switched from `<Link>` to `<button>` calling `open(item)`; both now `"use client"`. Dashboard and items layouts wrapped in `<ItemDrawerProvider>`. Sheet's built-in close X hidden (`showCloseButton={false}`) so it doesn't overlap the right-aligned Delete; closes via backdrop or Escape. Locale pinned to `"en-US"` in card/row/drawer date formatters to fix SSR/CSR hydration mismatch surfaced when those components became client. (Completed)
- **2026-05-09:** Item Drawer Edit Mode — pencil button toggles the drawer into an inline edit form. New `updateItem(itemId, userId, input)` in `src/lib/db/items.ts` updates fields and rewrites tag relations (`set: []` + `connectOrCreate`); shared `findItemDetail` helper now backs both `getItemById` and the post-update fetch. New Zod-validated `updateItem` server action in `src/actions/items.ts` (auth-checked, trims fields, dedupes tags, returns `{ success, data, error, fieldErrors? }`); 6 Vitest tests in `src/actions/items.test.ts` cover no-session, empty-title, invalid-url, not-found, trim/dedupe, and empty-string-to-null cases. New `ItemEditForm` (`src/components/items/ItemEditForm.tsx`) shows type-aware fields — Content (snippet/prompt/command/note), Language (snippet/command), URL (link), plus Title/Description/Tags for all — with Save/Cancel toolbar replacing the action bar; Save disabled when title empty; on success calls `router.refresh()`, fires a green success toast (`type="success"`) and returns to view mode; on error fires a red toast and surfaces field errors inline. `ItemDrawer.tsx` adds a `mode: "view" | "edit"` state and switches between `ActionBar`+`ItemBody` and `ItemEditForm`. New base-ui-backed `ToastProvider` in `src/components/ui/toast.tsx` (success = emerald, error = destructive); dashboard and items layouts wrapped in `<ToastProvider>` outside `<ItemDrawerProvider>`. (Completed)
- **2026-05-09:** Item Delete — trash button in the drawer action bar now opens a confirmation dialog. New shadcn-style `src/components/ui/alert-dialog.tsx` wraps `@base-ui/react/alert-dialog` (Root/Trigger/Portal/Overlay/Content/Header/Footer/Title/Description/Cancel/Action). New `deleteItem(itemId, userId)` in `src/lib/db/items.ts` uses `prisma.item.deleteMany({ where: { id, userId } })` so ownership scoping and the destructive op are atomic; `ItemCollection` rows cascade via the schema and M2M tag links detach automatically. New `deleteItem(itemId)` server action in `src/actions/items.ts` returns `{ success } | { success: false, error }` after `auth()` checks. `ItemDrawer.tsx` holds the dialog state, wires the trash button onClick, and on confirm runs the action via `useTransition`: success closes the drawer, fires a green toast, and `router.refresh()`es so the underlying card lists drop the row; failure fires a red toast and keeps the drawer open. 3 new Vitest tests in `src/actions/items.test.ts` cover no-session, not-found, and success paths (12 tests total now). (Completed)
- **2026-05-09:** Sidebar Brand Link — brand row in `SidebarContent.tsx` is now a `<Link href=/dashboard>` (was a static `<div>`), so clicking the D tile or DevBox wordmark navigates home from anywhere. Mobile drawer closes via the existing `onNavigate` prop. (Completed)
